# Version Bump Rationale

**Decision**: patch

This is a patch release. The only changes are a `dev_dependencies` update to `runtime_ci_tooling` in `pubspec.yaml`, which is considered a maintenance/chore task and does not affect the public API or features.

**Key Changes**:
- Bumped `runtime_ci_tooling` dev_dependency from `^0.9.0` to `^0.10.0` in `pubspec.yaml` to maintain consistency during workspace enablement flows.

**Breaking Changes**:
- None

**New Features**:
- None

**References**:
- chore: align runtime_ci_tooling dependency for workspace enablement