# Version Bump Rationale

**Decision**: patch
The changes since the last release consist entirely of maintenance chores, specifically updating the `.gitignore` file to ignore new local development artifacts (e.g., `.claude/`). There are no functional changes, new features, or breaking changes to the package's public API. According to the semantic versioning rules provided, maintenance and chore updates warrant a `patch` release.

**Key Changes**:
* Updated `.gitignore` to exclude new local tooling files.

**Breaking Changes**:
* None.

**New Features**:
* None.

**References**:
* `chore: update .gitignore with custom_lint.log, .dart_tool/, .claude/ entries`
