* The project's `.gitignore` has been updated to prevent accidental commits of local development environments and caching directories.
* Extraneous tooling artifacts such as `.claude/`, `.dart_tool/`, and `custom_lint.log` are now strictly ignored by version control.
* This maintenance patch ensures a cleaner workspace configuration for future contributors working with modern Dart and AI tooling.
